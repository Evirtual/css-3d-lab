{
  "name": "css-3d-lab",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-pages.mjs",
    "media": "node scripts/generate-media.mjs",
    "qa": "node scripts/qa.mjs",
    "compare": "node scripts/compare-capture.mjs",
    "icons": "node scripts/generate-icons.mjs",
    "dev": "npm run generate && vite",
    "build": "npm run generate && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^26.6.1",
    "playwright": "^1.63.0",
    "sass-embedded": "^1.104.1",
    "simple-icons": "^16.31.0",
    "typescript": "^7.0.2",
    "vite": "^8.3.0"
  },
  "dependencies": {
    "mp4-muxer": "^5.2.2",
    "webm-muxer": "^5.1.4"
  }
}
